<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">org.h2.Driver</property>
        <property name="connection.url">jdbc:h2:mem:hotel_db;DB_CLOSE_DELAY=-1</property>
        <property name="connection.username">sa</property>
        <property name="connection.password"></property>

        <!-- JDBC connection pool settings ... using built-in test pool -->
        <property name="connection.pool_size">1</property>

        <!-- Select our SQL dialect -->
        <property name="dialect">org.hibernate.dialect.H2Dialect</property>

        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>

        <!-- Update the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Annotated classes -->
        <mapping class="com.hotelmanagement.entity.Customer"/>
        <mapping class="com.hotelmanagement.entity.Room"/>
        <mapping class="com.hotelmanagement.entity.Bill"/>
        <mapping class="com.hotelmanagement.entity.FoodItem"/>
        <mapping class="com.hotelmanagement.entity.Manager"/>
        <mapping class="com.hotelmanagement.entity.Receptionist"/>
        <mapping class="com.hotelmanagement.entity.Inventory"/>
        <mapping class="com.hotelmanagement.entity.Owner"/>

    </session-factory>
</hibernate-configuration>
